% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cast.R
\name{resolve_rowlabel_join_cols}
\alias{resolve_rowlabel_join_cols}
\title{Locate the rowlabel columns to join summary statistics back on}
\usage{
resolve_rowlabel_join_cols(wide, by_labels, by_data_vars)
}
\arguments{
\item{wide}{Assembled wide table}

\item{by_labels}{Character vector of constant \code{by} labels}

\item{by_data_vars}{Character vector of \code{by} data variable names}
}
\value{
List with \code{tv_col} (the target variable's rowlabel column) and
\code{by_cols} (the by data variables' rowlabel columns, in \code{by_data_vars}
order), or NULL when \code{wide} has no rowlabel columns
}
\description{
The inverse of \code{build_row_labels_long()}'s layout: one rowlabel column
per constant \code{by} label first, then one per \code{by} data variable, then the
target variable last. Callers that merge per-group statistics (risk
difference, pairwise p-values) onto the assembled table need the data
variable columns; assuming they start at \code{rowlabel1} keys the join against a
constant-label column whenever \code{by} leads with a string label, matching
nothing and leaving every cell blank.
}
\keyword{internal}
