% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{check_denominator_integrity}
\alias{check_denominator_integrity}
\title{Warn when a nonzero count has a missing or zero denominator}
\usage{
check_denominator_integrity(counts, n_col, total_col, group_cols, layer_index)
}
\arguments{
\item{counts}{data.table holding the counts and their denominators}

\item{n_col}{Name of the count column (\code{"n"} or \code{"distinct_n"})}

\item{total_col}{Name of the denominator column}

\item{group_cols}{Columns identifying a row, used to name offending groups}

\item{layer_index}{Integer layer index}
}
\value{
Invisible TRUE
}
\description{
Denominators are attached with a left join, so a group present in the
analysis data but absent from the denominator source comes back with
\code{total = NA} and renders a blank-width percent (\code{" 5 (     )"}) —
and \code{total == 0} with \code{n > 0} used to display an affirmatively
wrong \code{0.0\%}. Both mean the denominator setup is wrong, so say so.
}
\keyword{internal}
