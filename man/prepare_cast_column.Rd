% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cast.R
\name{prepare_cast_column}
\alias{prepare_cast_column}
\title{Prepare the dcast column variable, respecting factor-level order}
\usage{
prepare_cast_column(dt, cols, col_levels = NULL)
}
\arguments{
\item{dt}{Long data.table about to be cast (mutated in place)}

\item{cols}{Character vector of column variable names}

\item{col_levels}{Named list from \code{get_col_levels()} (may be NULL/empty)}
}
\value{
The name of the variable to use on the RHS of the dcast formula
}
\description{
For a single column variable, converts it to a factor ordered by
\code{col_levels} so \code{dcast()} spreads columns in level order. For multiple
column variables, builds the \code{" | "}-joined interaction column and, when
any component is a factor, orders it by the cross-product of each
variable's level order (outermost variable varies slowest). When no
component is a factor the interaction is left as a character vector, so
\code{dcast()} falls back to alphabetical order exactly as before.
}
\keyword{internal}
