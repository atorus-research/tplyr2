# Build a single-target desc layer

Build a single-target desc layer

## Usage

``` r
build_desc_single(
  dt,
  tv,
  cols,
  by_data_vars,
  by_labels,
  settings,
  layer_index,
  col_n,
  var_label = NULL,
  var_index = NULL,
  pop_dt = NULL
)
```
