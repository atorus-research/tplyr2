# Create formatted rows for desc layer from pivoted ARD stats

Create formatted rows for desc layer from pivoted ARD stats

## Usage

``` r
create_desc_rows_from_ard(wide_stats, fmt_list, desc_group)
```
