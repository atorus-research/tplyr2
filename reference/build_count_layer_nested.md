# Process a nested (multi-variable) count layer

Process a nested (multi-variable) count layer

## Usage

``` r
build_count_layer_nested(
  dt,
  target_vars,
  cols,
  by_data_vars,
  by_labels,
  settings,
  layer_index,
  col_n = NULL,
  pop_dt = NULL
)
```
