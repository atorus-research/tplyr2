# Sort nested count data for correct interleaving

Sort nested count data for correct interleaving

## Usage

``` r
sort_nested(
  combined,
  target_vars,
  by_data_vars,
  dt = NULL,
  outer_sort_position = NULL,
  order_count_method = NULL,
  result_order_var = "n",
  ordering_cols = NULL,
  cols = character(0)
)
```
