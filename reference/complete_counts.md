# Complete count data to ensure all combinations exist

Complete count data to ensure all combinations exist

## Usage

``` r
complete_counts(
  counts,
  dt,
  cols,
  by_data_vars,
  tv,
  limit_data_by = NULL,
  denom_group = NULL
)
```
