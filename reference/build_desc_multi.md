# Build a multi-target desc layer

Build a multi-target desc layer

## Usage

``` r
build_desc_multi(
  dt,
  target_vars,
  cols,
  by_data_vars,
  by_labels,
  settings,
  layer_index,
  col_n,
  pop_dt = NULL
)
```
