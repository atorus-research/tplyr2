# Process a single-variable count layer

Process a single-variable count layer

## Usage

``` r
build_count_layer_single(
  dt,
  tv,
  cols,
  by_data_vars,
  by_labels,
  settings,
  layer_index,
  col_n = NULL,
  pop_dt = NULL
)
```
