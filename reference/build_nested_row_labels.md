# Build row labels for a nested count level

Build row labels for a nested count level

## Usage

``` r
build_nested_row_labels(
  counts,
  by_labels,
  by_data_vars,
  target_vars,
  level,
  n_levels,
  indentation
)
```
