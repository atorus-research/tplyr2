# Complete counts for a nested level

Complete counts for a nested level

## Usage

``` r
complete_nested_level(
  counts,
  dt,
  cols,
  by_data_vars,
  level_tvs,
  limit_data_by = NULL,
  denom_group = NULL
)
```
